<p id="notice"><%= notice %></p>

<h1>Noten</h1>

<table>
  <thead>
    <tr>
      <th>Note</th>
      <th>Gewichtung</th>
      <th>Kurztest</th>
      <th>Notenplan</th>
      <th>Fach</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @marks.each do |mark| %>
      <tr>
        <td><%= mark.mark %></td>
        <td><%= mark.weighting %></td>
        <td><%= mark.shorttest %></td>
        <td><%= mark.notenplan %></td>
        <td><%= mark.subject %></td>
        <td><%= link_to 'Anzeigen', mark %></td>
        <td><%= link_to 'Bearbeiten', edit_mark_path(mark) %></td>
        <td><%= link_to 'Löschen', mark, method: :delete, data: { confirm: 'Sind sie sicher?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Neue Note', new_mark_path %>
